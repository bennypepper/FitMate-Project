# Phase 6: Integration & Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 06-integration-polish
**Areas discussed:** WhatsApp CTA, PWA Installability, Mobile Responsiveness, Offline Behavior

---

## WhatsApp CTA Placement & Message Content

| Option | Description | Selected |
|--------|-------------|----------|
| Show always | CTA visible on both clean and toxic results | |
| Show only when toxic detected | CTA appears inside ToxicityWarning card only when danger found | ✓ |

**User's choice:** Show only when toxic/contraindicated ingredients are detected — inside the ToxicityWarning Imperial Red card.

**Notes:**
- WA number confirmed: `6285161618852` (Indonesian mobile)
- Pre-filled message dynamically injects detected ingredient names
- Button opens wa.me deep link in new tab

---

## PWA Icons & Installability

| Option | Description | Selected |
|--------|-------------|----------|
| Placeholder icon | Simple letter/monogram generated for demo | |
| Use existing logo | `logo2.png` from OneDrive assets (leaf+scanner mark on Imperial Red gradient) | ✓ |

**User's choice:** Use `logo2.png` — Imperial Red gradient rounded-square icon with leaf/scanner mark and golden bracket corners. Already copied to `frontend/public/icon.png`.

**Notes:**
- User asked what a splash screen is → explained: auto-generated by browser from manifest `background_color` + 512px icon, no extra code
- User asked what "installable from scanner page" means → explained: browser install prompt appears when manifest + service worker + icons are valid
- Splash screen: Yes, include (automatic via manifest)
- Installable: Yes, on mobile

---

## Mobile Responsiveness

| Option | Description | Selected |
|--------|-------------|----------|
| Full responsive redesign | Redo layouts for mobile | |
| Audit pass + targeted fixes | Test at 375px/393px viewports, fix what's broken | ✓ |

**User's choice:** Run responsive audit pass. User reported error during localhost testing: `TypeError: Failed to fetch` — identified as backend not running, not a frontend layout issue. Captured as D-11/D-12/D-13 (error handling improvement).

**Notes:**
- User couldn't test responsive layout because backend was down
- Root cause: `fetch("http://localhost:8000/...")` throws when backend not running
- Fix: replace `alert()` with inline error state + `navigator.onLine` check

---

## Service Worker & Offline Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Full offline caching | Cache shell + assets for offline use | |
| Minimal SW (install only) + error state | Register SW for PWA badge, show "no internet" message on scan failure | ✓ |

**User's choice:** Minimal service worker for installability only. Show clear Indonesian-language error message when offline or server unreachable. No offline functionality.

---

## the Agent's Discretion

- Icon resize approach (sharp script, canvas, or build step)
- WhatsApp SVG icon in CTA button
- Exact animation on error card
- Toast vs inline card for network errors

## Deferred Ideas

- Real offline caching — explicitly decided against
- PWA screenshots for richer install dialog — skip for prototype
- Push notifications — out of scope v1
