---
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/results/ToxicityWarning.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - WHAP-01
---

# Plan 06-01: WhatsApp Deep Link Integration

## Objective
Wire the pre-filled stateless WhatsApp deep link CTA to the scanner results.

## Tasks

```xml
<task>
<id>06-01-01</id>
<title>Add WhatsApp CTA to ToxicityWarning</title>
<read_first>
- frontend/src/components/results/ToxicityWarning.tsx
</read_first>
<action>
1. Identify the bottom of the `ToxicityWarning` component (after the ingredients list).
2. Render a full-width WhatsApp CTA button only if `toxicItems` has items.
3. The button should have:
   - `href="https://wa.me/6285161618852?text=" + encodeURIComponent("Halo FitMate! Saya baru scan produk TCM dan ditemukan bahan berbahaya: " + toxicItems.map(item => item.indonesian_name || item.mandarin_name).join(', ') + ". Bisa bantu jelaskan risikonya?")`
   - `target="_blank"` and `rel="noopener noreferrer"`
   - Text reading: "Tanya di WhatsApp"
   - Tailwind styling: `w-full bg-white text-[#930014] rounded-xl flex items-center justify-center p-3 mt-4 text-center font-bold shadow-sm transition active:scale-95`
4. Use a WhatsApp logo or a simple chat bubble icon inside the button.
</action>
<acceptance_criteria>
- `frontend/src/components/results/ToxicityWarning.tsx` contains `wa.me/6285161618852`
- `frontend/src/components/results/ToxicityWarning.tsx` contains `encodeURIComponent`
- `frontend/src/components/results/ToxicityWarning.tsx` contains `toxicItems.map`
</acceptance_criteria>
</task>

<task>
<id>06-01-02</id>
<title>Make API URL Configurable and Implement UI for Connection Error</title>
<read_first>
- frontend/src/app/page.tsx
</read_first>
<action>
1. In `frontend/src/app/page.tsx`, replace all hardcoded occurrences of `http://localhost:8000` with `process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000"`.
2. Locate the `handleImageReady` function's `catch (error)` block.
3. Replace `alert(error)` with setting a state variable (e.g., `const [networkError, setNetworkError] = useState(null)`). 
   - If `!navigator.onLine`, set error to `"Tidak ada koneksi internet atau server tidak dapat dijangkau. Pastikan Anda terhubung ke internet untuk menggunakan fitur scanning."`
   - Else, set error to `"Server tidak dapat dijangkau. Coba lagi dalam beberapa saat."`
4. Conditionally render an Error Card in the UI if `networkError` is truthy. The Error Card should have a red/white design, display the `networkError` text, and have a `Coba Lagi` button that calls exactly what `handleReset` calls (clearing the error and result states).
</action>
<acceptance_criteria>
- `frontend/src/app/page.tsx` contains `process.env.NEXT_PUBLIC_API_URL`
- `frontend/src/app/page.tsx` does NOT contain `alert(` in the API catch block
- `frontend/src/app/page.tsx` contains `navigator.onLine`
- `frontend/src/app/page.tsx` contains `Tidak ada koneksi internet atau server tidak dapat dijangkau`
</acceptance_criteria>
</task>
```
