---
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/requirements.txt
  - backend/database/mongo.py
  - backend/services/safety.py
  - backend/routers/analyze.py
  - backend/main.py
autonomous: true
requirements_addressed:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
---

# Plan 02-02: Rule-Based Safety Engine

## Objective
Implement toxicity matching logic using fuzzy-matching against MongoDB and create the combined analysis endpoint returning severity-grouped warnings and a medical disclaimer.

## Tasks

<task>
<action>
Set up MongoDB connection.
- Update `backend/requirements.txt` to add `motor` (for exact async MongoDB logic) and `thefuzz` / `python-Levenshtein`.
- Create `backend/database/mongo.py` establishing an async `AsyncIOMotorClient` connection using `MONGODB_URI` from env.
- Expose a `get_db()` helper returning the targeted database.
</action>
<read_first>
- backend/requirements.txt
</read_first>
<acceptance_criteria>
- `backend/requirements.txt` contains MongoDB async driver and fuzzy matching library
- `backend/database/mongo.py` successfully exposes `get_db()`
</acceptance_criteria>
</task>

<task>
<action>
Implement rule-based safety matching using fuzzy search.
- Create `backend/services/safety.py` with function `match_ingredients(detected_texts: list[str], db_conn) -> dict`
- In the logic, query the `ingredients` collection and use fuzzy matching (via `thefuzz` or similar on `mandarin_name`) (per D-03) against the user incoming text strings.
- Separate matched ingredients into severity groups dictionary (e.g., `{"toxic": [], "contraindicated": [], "safe": [], "unknown": []}`) (per D-04).
- Ensure output fields `target_organ` and `risk_level` are included for any flagged items according to SAFE-03.
</action>
<read_first>
- .planning/phases/02-backend-core/02-CONTEXT.md
</read_first>
<acceptance_criteria>
- `backend/services/safety.py` implements a fuzzy matching algorithm.
- Returned result structure uses a categorized dictionary grouped by severity level.
</acceptance_criteria>
</task>

<task>
<action>
Create Analysis API Endpoint.
- Create `backend/routers/analyze.py` with `POST /analyze` accepting `file: UploadFile = File(...)`
- Combine the operations in this endpoint:
  1. Process the image via `extract_and_translate_text`
  2. Extract raw strings and pass into `match_ingredients`
  3. Combine the results, bounding boxes, and grouped warnings into the final JSON.
  4. Ensure a hardcoded medical disclaimer string is injected in the response payload (SAFE-04).
- Add router to `backend/main.py`
</action>
<read_first>
- backend/routers/ocr.py
- .planning/phases/02-backend-core/02-CONTEXT.md
</read_first>
<acceptance_criteria>
- `backend/routers/analyze.py` integrates both vision and safety logic.
- The returned JSON contains bounding boxes, grouped toxicity results, and explicit `disclaimer` field string.
- Router is included in `main.py`.
</acceptance_criteria>
</task>

## Verification
- Ensure `POST /api/v1/analyze/` returns structured severity JSON and a medical disclaimer.

## Must Haves
- Fuzzy matching handles OCR inaccuracies (D-03).
- Warnings grouped by severity level (D-04).
- Medical disclaimer strings are present in every result (SAFE-04).
