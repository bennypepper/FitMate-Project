---
wave: 3
depends_on: [03-02]
files_modified:
  - frontend/app/page.tsx
  - frontend/components/results/ResultsCard.tsx
  - frontend/components/results/ToxicityWarning.tsx
  - frontend/components/results/IngredientList.tsx
autonomous: false
requirements:
  - PWA-02
---

# Phase 03-03: Results Display & API Integration

<objective>
Wire the frontend to the backend FastAPI OCR/Safety engine, format the results, and strictly display Imperial Red warnings and professional disclaimers.
</objective>

<must_haves>
- Concrete API call integrating `fetch` against the FastAPI `POST /api/v1/scan` endpoint.
- `ResultsCard.tsx`: Root level presentation showing the extracted herbs.
- `ToxicityWarning.tsx`: Component displaying High/Medium risk alerts using Imperial Red (`#930014`).
- Medical disclaimer explicitly shown on the UI.
</must_haves>

<deep_work_rules>
- Every task MUST include `<read_first>`, `<action>`, and `<acceptance_criteria>`.
- The `<action>` MUST be highly explicit and concrete.
- The `<acceptance_criteria>` MUST be mechanically verifiable (grep or CLI output).
</deep_work_rules>

## Tasks

### 1. Build Toxicity Warning Component
```xml
<task>
  <read_first>
    - ../../stitch_pkm_ki_fitme_v1/modern_apothecary/DESIGN.md
  </read_first>
  <action>
    Create `frontend/components/results/ToxicityWarning.tsx`.
    - Accepts props `ingredients: any[]` containing warning data.
    - Filter ingredients where `is_toxic` is true.
    - If none, return null or a safe message in `text-surface_on`.
    - For toxic items, render a card with `bg-primary_container` (`#930014`) and `text-white` (or light variant) to scream danger implicitly.
    - Display target organ and risk severity prominently.
  </action>
  <acceptance_criteria>
    - `grep -q "bg-primary_container" frontend/components/results/ToxicityWarning.tsx` exits 0
    - `grep -q "is_toxic" frontend/components/results/ToxicityWarning.tsx` exits 0
  </acceptance_criteria>
</task>
```

### 2. Build Ingredient List & Medical Disclaimer
```xml
<task>
  <read_first>
    - frontend/components/results/ToxicityWarning.tsx
  </read_first>
  <action>
    Create `frontend/components/results/IngredientList.tsx`.
    - Takes full array of parsed ingredients (Mandarin and Indonesian translation).
    - Formats using pills or lists applying `surface_container_low` for regular items.
    
    Create `frontend/components/results/ResultsCard.tsx`.
    - Wraps `ToxicityWarning` and `IngredientList`.
    - Displays a sticky/prominent disclaimer text: 
      "INFORMASI INI HANYA UNTUK TUJUAN EDUKASI DAN BUKAN PENGGANTI SARAN MEDIS PROFESIONAL. KONSULTASIKAN DENGAN DOKTER ATAU APOTEKER."
    - Stylized subtly using `text-sm text-on_surface_variant` below the results.
  </action>
  <acceptance_criteria>
    - `grep -q "INFORMASI INI HANYA UNTUK TUJUAN EDUKASI" frontend/components/results/ResultsCard.tsx` exits 0
    - `grep -q "<IngredientList" frontend/components/results/ResultsCard.tsx` exits 0
  </acceptance_criteria>
</task>
```

### 3. Integrate Backend API Call
```xml
<task>
  <read_first>
    - frontend/app/page.tsx
  </read_first>
  <action>
    Modify `frontend/app/page.tsx` backend integration:
    - Replace the 2s timeout from the capture process with a true `fetch` definition hitting FastAPI (e.g., `http://localhost:8000/api/v1/scan`).
    - Use `FormData` to append the base64 (`file` blob) or json depending on backend expectations (Wait: backend expects multipart/form-data with an image file).
    - Convert base64 dataURL to Blob: `fetch(base64).then(res => res.blob())` and append as `file`.
    - Extract JSON response: `{ text_detected: string, matched_ingredients: array, disclaimer: string }`.
    - Render `<ResultsCard>` conditionally displaying these results instead of the scanner when valid.
  </action>
  <acceptance_criteria>
    - `grep -q "/api/v1/scan" frontend/app/page.tsx` exits 0
    - `grep -q "FormData" frontend/app/page.tsx` exits 0
  </acceptance_criteria>
</task>
```

<verification>
- End-to-end frontend to backend data flow properly triggers.
</verification>
