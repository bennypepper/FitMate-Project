---
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/components/scanner/CameraViewfinder.tsx
  - frontend/components/scanner/UploadFallback.tsx
  - frontend/components/scanner/ProcessingLoader.tsx
  - frontend/app/page.tsx
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
---

# Phase 03-02: Camera Module & Capture UX

<objective>
Implement the custom HTML5 camera viewfinder, gallery upload fallback, and the processing state loader with visual Hanzi bounding box animations.
</objective>

<must_haves>
- `CameraViewfinder.tsx`: React component capturing video stream via `navigator.mediaDevices.getUserMedia(video)`.
- `UploadFallback.tsx`: Input type="file" for gallery uploads.
- `ProcessingLoader.tsx`: A glassmorphism generalized loading overlay with fake Hanzi bounding box animations (simulating OCR).
- Main scanner page layout.
</must_haves>

<deep_work_rules>
- Every task MUST include `<read_first>`, `<action>`, and `<acceptance_criteria>`.
- The `<action>` MUST be highly explicit and concrete (e.g., exact hook implementations).
- The `<acceptance_criteria>` MUST be mechanically verifiable (grep or CLI output).
</deep_work_rules>

## Tasks

### 1. Build Camera Viewfinder Component
```xml
<task>
  <read_first>
    - .planning/phases/03-frontend-scanner/03-CONTEXT.md (D-01)
  </read_first>
  <action>
    Create `frontend/components/scanner/CameraViewfinder.tsx`.
    - Implement a `useRef<HTMLVideoElement>` to attach the camera stream.
    - On mount, call `navigator.mediaDevices.getUserMedia({ video: { facingMode: "environment" } })` and set the stream to the video ref.
    - Provide a "Capture" button styled gracefully: `bg-tertiary_container text-on_tertiary_fixed rounded-md`.
    - On capture, draw the `video` element onto an offscreen `<canvas>` and extract base64 via `canvas.toDataURL('image/jpeg')`.
    - Add error handling for denied permissions.
  </action>
  <acceptance_criteria>
    - `grep -q "getUserMedia" frontend/components/scanner/CameraViewfinder.tsx` exits 0
    - `grep -q "facingMode" frontend/components/scanner/CameraViewfinder.tsx` exits 0
    - `grep -q "toDataURL" frontend/components/scanner/CameraViewfinder.tsx` exits 0
  </acceptance_criteria>
</task>
```

### 2. Build Upload Fallback Component
```xml
<task>
  <read_first>
    - frontend/components/scanner/CameraViewfinder.tsx
  </read_first>
  <action>
    Create `frontend/components/scanner/UploadFallback.tsx`.
    - Create a file input: `<input type="file" accept="image/*" />`.
    - Use a FileReader to convert the selected file to a base64 string on change.
    - Propagate the base64 string to a parent callback `onImageReady(base64)`.
    - Style as a subtle secondary button with `bg-surface_container_highest` and `text-on_surface` rounding `md`.
  </action>
  <acceptance_criteria>
    - `grep -q "type=\"file\"" frontend/components/scanner/UploadFallback.tsx` exits 0
    - `grep -q "readAsDataURL" frontend/components/scanner/UploadFallback.tsx` exits 0
  </acceptance_criteria>
</task>
```

### 3. Build Processing Loader State Component
```xml
<task>
  <read_first>
    - .planning/phases/03-frontend-scanner/03-CONTEXT.md (D-02)
  </read_first>
  <action>
    Create `frontend/components/scanner/ProcessingLoader.tsx`.
    - Create a full-screen or card overlay with Glassmorphism using Tailwind: `bg-surface/80 backdrop-blur-[20px]`.
    - Inside the loader, implement a looping CSS animation or structured SVG simulating bounding boxes hunting for text (visual Hanzi scanner).
    - Display text "Menganalisis Komposisi TCM...".
  </action>
  <acceptance_criteria>
    - `grep -q "backdrop-blur" frontend/components/scanner/ProcessingLoader.tsx` exits 0
    - `grep -q "Menganalisis" frontend/components/scanner/ProcessingLoader.tsx` exits 0
  </acceptance_criteria>
</task>
```

### 4. Assemble Main Scanner Page
```xml
<task>
  <read_first>
    - frontend/app/page.tsx
    - frontend/components/scanner/CameraViewfinder.tsx
  </read_first>
  <action>
    Modify `frontend/app/page.tsx`.
    - Implement state `const [image, setImage] = useState<string | null>(null)`.
    - Implement state `const [isProcessing, setIsProcessing] = useState(false)`.
    - Render `<CameraViewfinder>` by default, with `<UploadFallback>` below it.
    - If `isProcessing` is true, overlay `<ProcessingLoader>`.
    - On `onImageReady` from viewfinder or fallback, set `isProcessing(true)` and simulate a 2s timeout for now (API call placeholder).
  </action>
  <acceptance_criteria>
    - `grep -q "<CameraViewfinder" frontend/app/page.tsx` exits 0
    - `grep -q "<ProcessingLoader" frontend/app/page.tsx` exits 0
  </acceptance_criteria>
</task>
```

<verification>
- Navigate to `/` on mobile/desktop and verify camera permission is requested.
- Clicking capture sets processing state visually.
</verification>
